---
interface Props {
	grid: number[];
}

const { grid } = Astro.props;
---

{grid.map((left) => (
	<div class="absolute inset-y-0 border-l border-rule" style={`left: ${left}%`} />
))}
<div class="absolute inset-y-0 right-0 border-l border-rule"></div>
